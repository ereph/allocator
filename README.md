# Allocator
A simple, robust, and readable heap allocator.

## TODO

- Add segment slicing.
- Coalescing of segments
- Optimal fit until X sorting algorithm.
- Adding thread compatability
- Benchmark fragmentation, latency, etc compared to other allocators.
- Modify my CMakeLists.txt to port this allocator into a library file for ease of use later on. 
